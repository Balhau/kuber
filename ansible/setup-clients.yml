- name: Setup Kubernetes Clients
  hosts: raspcluster
  become: True

  tasks:
    - name: Add Kubernetes Apt Key
      apt_key:
        url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
    
    - name: Add Kubernetes Apt Repository
      apt_repository:
        repo: deb http://apt.kubernetes.io/ kubernetes-xenial main
        state: present
