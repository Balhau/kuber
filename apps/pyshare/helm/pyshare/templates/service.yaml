apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyshare.fullname" . }}
  labels:
    {{- include "pyshare.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyshare.selectorLabels" . | nindent 4 }}
