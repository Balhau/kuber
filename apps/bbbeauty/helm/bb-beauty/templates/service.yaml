apiVersion: v1
kind: Service
metadata:
  name: {{ include "bb-beauty.fullname" . }}
  labels:
    {{- include "bb-beauty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bb-beauty.selectorLabels" . | nindent 4 }}
