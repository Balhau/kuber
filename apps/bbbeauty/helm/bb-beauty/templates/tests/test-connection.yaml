apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bb-beauty.fullname" . }}-test-connection"
  labels:
    {{- include "bb-beauty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bb-beauty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
