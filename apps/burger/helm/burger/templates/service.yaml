apiVersion: v1
kind: Service
metadata:
  name: {{ include "burger.fullname" . }}
  labels:
    {{- include "burger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "burger.selectorLabels" . | nindent 4 }}
