apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bt-tracker.fullname" . }}-test-connection"
  labels:
    {{- include "bt-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bt-tracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
