apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-pt.fullname" . }}-test-connection"
  labels:
    {{- include "web-pt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-pt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
